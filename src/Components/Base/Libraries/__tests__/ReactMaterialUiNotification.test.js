let wrapper
describe('Components/Libraries/ReactMaterialUiNotifications', function () {
    beforeEach(() => {
        wrapper = require('../ReactMaterialUiNotifications')
    })
    it('should export object', function () {
        expect(wrapper).toBeTruthy()
    })
})