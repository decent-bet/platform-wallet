import React from 'react'
import { shallow } from 'enzyme'

//import LoginInner from '../LoginInner'
/* TODO: call LoginInner to get a testable component
* LoginInner({
    loginType,
    mnemonic,
    privateKey,
    password,
    confirmPassword,
    onLoginTypeChangedListener,
    onMnemonicChangedListener,
    onKeyPressedListener,
    onPasswordChangedListener,
    onPasswordConfirmationChangedListener
})
* */

describe.skip('Components/Login/LoginInner', function() {
    it('should render without throwing an error', function () {
        // const wrapper = shallow(LoginInner)
        // TODO: flesh out
    })
})