import React from 'react'
import { shallow } from 'enzyme'

import App from '../App'

describe.skip('Components/App', function() {
    it('should render without throwing an error', function() {
        const wrapper = shallow(<App />)
        // TODO: flesh out
    })
})
